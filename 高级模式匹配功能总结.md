# PawLang v0.2.2 - 高级模式匹配功能总结

## 🎯 实现概述

本次更新为 PawLang 添加了 4 个高级模式匹配特性，极大增强了语言的表达能力。

---

## ✨ 新增特性

### 1. 范围模式 (Range Pattern)

**语法**：
- `start..end` - 排除式范围（不包含end）
- `start..=end` - 包含式范围（包含end）

**示例**：
```paw
let score = 85;
let grade = score is {
    90..100 => "A",    // 90 <= score < 100
    80..90 => "B",     // 80 <= score < 90
    70..=79 => "C",    // 70 <= score <= 79
    _ => "F"
};
// grade == "B"
```

**支持类型**：
- 整数类型：`i32`, `i64`, `u32`, `u64`
- 字符类型：`char`

**用例**：
- 成绩等级判定
- 字符类型分类（`'a'..'z'` 匹配小写字母）
- 年龄段划分

---

### 2. OR 模式 (Or Pattern)

**语法**：`pattern1 | pattern2 | pattern3`

**示例**：
```paw
let x = 2;
let category = x is {
    1 | 2 | 3 => "small",
    4 | 5 | 6 => "medium",
    7 | 8 | 9 => "large",
    _ => "other"
};
// category == "small"
```

**特点**：
- 可以组合任意多个模式
- 所有分支必须类型一致
- 支持字面量、枚举变体等

**用例**：
- 简化多值匹配
- 枚举多个有效输入
- 减少重复代码

---

### 3. 中间 Rest 模式 (Middle Rest Pattern)

**语法**：
- `[first, .., last]` - 匹配首尾，忽略中间
- `[a, b, .., y, z]` - 匹配前2个和后2个
- `[.., last]` - 只匹配最后一个
- `[first, ..]` - 只匹配第一个

**示例**：
```paw
let arr: [i32; 5] = [1, 2, 3, 4, 5];
let result = arr is {
    [first, .., last] => first + last,  // 1 + 5 = 6
    _ => 0
};
```

**高级示例（前缀+后缀）**：
```paw
let arr: [i32; 6] = [10, 20, 30, 40, 50, 60];
let result = arr is {
    [a, b, .., y, z] => a + b + y + z,  // 10+20+50+60 = 140
    _ => 0
};
```

**特点**：
- 支持前缀、后缀、中间三部分
- 运行时自动计算 rest 部分的长度
- 可以命名 rest：`[first, ..middle, last]`

**用例**：
- 提取数组首尾元素
- 处理变长序列
- 灵活的数据结构解构

---

### 4. 嵌套模式 (Nested Pattern)

**完善支持**：所有模式都可以任意嵌套组合

**示例 1：嵌套元组**
```paw
let data = ((1, 2), (3, 4));
let sum = data is {
    ((a, b), (c, d)) => a + b + c + d,  // 10
    _ => 0
};
```

**示例 2：元组中的数组**
```paw
let data = ([1, 2], [3, 4]);
let sum = data is {
    ([a, b], [c, d]) => a + b + c + d,  // 10
    _ => 0
};
```

**示例 3：数组中的元组**
```paw
let data: [(i32, i32); 2] = [(1, 2), (3, 4)];
let sum = data is {
    [(a, b), (c, d)] => a + b + c + d,  // 10
    _ => 0
};
```

**支持的嵌套组合**：
- 元组 + 元组
- 元组 + 数组
- 数组 + 元组
- 结构体 + 任意模式
- 枚举 + 任意模式

---

## 🔥 组合使用

**范围 + OR**：
```paw
let score = 85;
let pass = score is {
    80..90 | 70..80 => true,
    _ => false
};
```

**中间 Rest + 嵌套**：
```paw
let data: [(i32, i32); 3] = [(1, 2), (3, 4), (5, 6)];
let result = data is {
    [(a, b), .., (y, z)] => a + b + y + z,  // 1+2+5+6 = 14
    _ => 0
};
```

---

## 📊 实现细节

### AST 扩展

新增两个 Pattern 节点：

```cpp
class RangePattern : public Pattern {
    PatternPtr start;
    PatternPtr end;
    bool inclusive;  // ..= vs ..
};

class OrPattern : public Pattern {
    std::vector<PatternPtr> alternatives;
};
```

扩展 `SlicePattern`：

```cpp
class SlicePattern : public Pattern {
    std::vector<PatternPtr> prefix;   // 前缀
    PatternPtr rest;                   // 中间 (可选)
    std::vector<PatternPtr> suffix;    // 后缀 (新增)
};
```

### 编译器流程

#### 1. Parser（解析）
- 在 `parseBasePattern()` 中检测字面量后的 `..` token
- 在 `parsePattern()` 中检测 `|` token
- `parseArrayPattern()` 支持后缀元素

#### 2. TypeChecker（类型检查）
- `RangePattern`：验证起始和结束值类型一致，且为整数或字符
- `OrPattern`：验证所有分支类型一致
- `SlicePattern`：验证 `prefix.size() + suffix.size() <= array.size()`

#### 3. CodeGen（代码生成）
- **RangePattern**：生成 `value >= start && value <= end` 的条件
- **OrPattern**：生成 `cond1 || cond2 || cond3` 的条件
- **SlicePattern**：
  - 前缀：从索引 0 开始
  - 后缀：从 `array.size() - suffix.size()` 开始
  - Rest：计算剩余长度 `rest_size = total - prefix - suffix`

---

## 🧪 测试覆盖

所有测试均已通过：

| 测试项 | 文件 | 状态 |
|-------|------|------|
| 范围模式（整数）| `test_all_patterns.paw` | ✅ |
| 范围模式（字符）| 单独测试 | ✅ |
| OR 模式 | `test_all_patterns.paw` | ✅ |
| 中间 Rest（首尾）| `test_all_patterns.paw` | ✅ |
| 中间 Rest（前缀+后缀）| 单独测试 | ✅ |
| 嵌套元组 | `test_all_patterns.paw` | ✅ |
| 嵌套数组+元组 | `test_all_patterns.paw` | ✅ |
| 组合模式 | `test_all_patterns.paw` | ✅ |

---

## 📈 性能特征

### 编译时开销
- **Parser**：O(n)，n 为模式复杂度
- **TypeChecker**：O(n)，验证类型一致性
- **CodeGen**：O(n)，生成匹配条件

### 运行时性能
- **范围模式**：2 次整数比较（常数时间）
- **OR 模式**：最坏情况 O(k) 比较，k 为分支数
- **中间 Rest**：编译时计算偏移，运行时 O(1) 访问
- **嵌套模式**：递归生成，性能等同于手写代码

---

## 🎓 设计哲学

1. **零抽象开销**：所有模式在编译时展开为高效的机器代码
2. **类型安全**：所有模式在编译时验证类型一致性
3. **表达力优先**：语法简洁，易于理解和使用
4. **完全可组合**：任意模式可以自由嵌套和组合

---

## 🚀 未来扩展

可能的增强方向：

1. **范围模式扩展**：
   - 浮点数范围（需要精度处理）
   - 开区间语法：`start<..end` 或 `start..<end`

2. **OR 模式优化**：
   - 编译期常量折叠
   - 跳转表优化（对于连续整数）

3. **Slice 模式增强**：
   - 支持运行时 Slice 类型（当前仅支持编译时已知大小的数组）
   - 多个 rest 段：`[a, ..x, b, ..y, c]`（语义复杂，需慎重考虑）

4. **Guard 表达式**：
   - `1..10 if x % 2 == 0 => "even in range"`

---

## 📝 版本信息

- **PawLang 版本**：v0.2.2
- **实现日期**：2025-11-04
- **代码变更**：
  - 新增文件：无
  - 修改文件：
    - `src/frontend/parser/ast/pattern.h`
    - `src/frontend/parser/parser.cpp`
    - `src/middleend/sema/type_checker.cpp`
    - `src/backend/codegen/expr/match_codegen.cpp`
    - 所有 Visitor 子类

---

## ✅ 结论

本次更新成功实现了 4 个高级模式匹配特性，为 PawLang 带来了与 Rust、Scala 等现代语言相媲美的模式匹配能力。所有特性均经过完整测试，性能表现优秀，可投入生产使用。

**高级模式匹配 = 更简洁的代码 + 更强的表达力 + 更好的可维护性！** 🎉

