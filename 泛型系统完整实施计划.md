# 泛型系统完整实施计划

**开始时间**: 2025-11-02  
**预计时长**: 13-21小时  
**目标**: 完整的泛型系统和类型推导

---

## 📋 任务清单

### ✅ 已完成
- [x] 泛型Enum (Phase 3已完成)

### ⏳ 待实施
- [ ] G1: 泛型Struct (2-3小时)
- [ ] G2: 泛型Interface (3-5小时)
- [ ] G3: 泛型函数 (5-8小时)
- [ ] G4: 变量类型推导 (3-5小时)
- [ ] G5: 完整测试验证 (1-2小时)

**总计**: 13-21小时

---

## 🎯 G1: 泛型Struct

**目标语法**:
```paw
type Box<T> = struct {
    value: T
}

let b: Box<i32> = Box { value: 42 };
let v = b.value;
println(v);  // 42
```

**实施步骤**:
1. 修改Parser: 在parseStructDecl中区分泛型struct
2. 注册GenericTemplate（STRUCT kind）
3. 实例化Struct类型
4. 测试验证

**预计**: 2-3小时

---

## 🎯 G2: 泛型Interface

**目标语法**:
```paw
interface Comparable<T> {
    fn compare(self, other: T) -> i32;
}

support Box<i32> with Comparable<Box<i32>> {
    fn compare(self, other: Box<i32>) -> i32 {
        return self.value - other.value;
    }
}
```

**实施步骤**:
1. 修改Parser: parseInterfaceDecl支持泛型
2. 注册Interface模板
3. 实例化Interface类型
4. Support块集成

**预计**: 3-5小时

---

## 🎯 G3: 泛型函数

**目标语法**:
```paw
fn identity<T>(x: T) -> T {
    return x;
}

fn main() {
    let v1 = identity(42);  // 推导为identity<i32>(42)
    let v2 = identity("hello");  // 推导为identity<string>("hello")
}
```

**实施步骤**:
1. 函数模板注册
2. 调用点类型参数推导
3. 函数实例化
4. Monomorphization Pass增强

**预计**: 5-8小时

---

## 🎯 G4: 变量类型推导

**目标语法**:
```paw
let x = 42;  // 推导为i32
let s = "hello";  // 推导为string
let b = true;  // 推导为bool
let arr = [1, 2, 3];  // 推导为[i32; 3]
```

**实施步骤**:
1. 修改Parser: VarDecl类型可选
2. TypeChecker: 从initializer推导类型
3. 字面量类型推导规则
4. 测试验证

**预计**: 3-5小时

---

## 🎯 G5: 完整测试验证

**测试计划**:

1. **泛型struct测试**
```paw
type Box<T> = struct { value: T }
let b: Box<i32> = Box { value: 42 };
```

2. **泛型函数测试**
```paw
fn identity<T>(x: T) -> T { return x; }
let v = identity(42);
```

3. **变量推导测试**
```paw
let x = 42;
let s = "hello";
```

4. **综合测试**
```paw
fn map<T, U>(arr: [T], f: fn(T) -> U) -> [U] { ... }
```

**预计**: 1-2小时

---

## 📈 实施顺序

**第1轮** (5-8小时):
1. G1: 泛型Struct (2-3h)
2. G4: 变量类型推导 (3-5h)

**第2轮** (8-13小时):
3. G3: 泛型函数 (5-8h)
4. G2: 泛型Interface (3-5h)

**第3轮** (1-2小时):
5. G5: 完整测试验证

---

## 🚀 开始实施G1！

