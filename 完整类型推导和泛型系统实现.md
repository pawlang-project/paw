# 完整类型推导和泛型系统实现

**日期**: 2025-11-02  
**预计工作量**: 8-13小时  
**目标**: 实现生产级的类型推导和泛型系统

---

## 🎯 实施路线图

### Phase 1: 准备工作（30分钟）

- [x] 修复closure_demo.paw基础语法
- [ ] 建立测试用例
- [ ] 架构设计文档

---

### Phase 2: 闭包参数类型推导（3-5小时）

#### 2.1 Parser改进 (1小时)

**目标**: 支持无类型参数 `(x, y) { ... }`

**实现**:
```cpp
// parser.cpp::parseClosureExpr()
std::vector<ClosureParam> parseClosureParams(bool allow_no_type = false) {
    // 解析参数，允许无类型
    if (!match(COLON)) {
        if (allow_no_type) {
            param.type = nullptr;  // 延迟推导
        } else {
            error("Expected type annotation");
        }
    }
}
```

#### 2.2 TypeChecker双向推导 (2-3小时)

**核心算法**: Bidirectional Type Checking

**实现**:
```cpp
void TypeChecker::visit(VarDecl* node) {
    // 如果有显式类型注解
    if (node->getType()) {
        expected_type_ = node->getType();
    }
    
    // 检查初始值
    if (node->getInit()) {
        node->getInit()->accept(this);
        
        // 如果初始值是闭包且有expected_type_
        if (auto* closure = dynamic_cast<ClosureExpr*>(node->getInit())) {
            if (expected_type_ && expected_type_->isFunction()) {
                // 推导闭包参数类型
                inferClosureParamTypes(closure, expected_type_);
            }
        }
    }
}

void TypeChecker::inferClosureParamTypes(ClosureExpr* closure, Type* expected_fn_type) {
    auto* fn_type = static_cast<FunctionType*>(expected_fn_type);
    const auto& expected_params = fn_type->getParamTypes();
    auto& closure_params = closure->getParams();
    
    for (size_t i = 0; i < closure_params.size(); i++) {
        if (!closure_params[i].type && i < expected_params.size()) {
            closure_params[i].type = expected_params[i];  // 推导！
        }
    }
}
```

#### 2.3 测试验证 (30分钟)

```paw
let add: fn(i32, i32) -> i32 = (x, y) { x + y };
// x, y自动推导为i32
```

---

### Phase 3: 完整泛型系统（5-10小时）

#### 3.1 架构设计 (1-2小时)

**核心组件**:

1. **GenericTemplate** - 泛型模板
```cpp
struct GenericTemplate {
    std::string name;                    // "Option"
    std::vector<std::string> type_params;  // ["T"]
    ASTNode* definition;                  // Enum/Struct定义
};
```

2. **实例化表** - 缓存已实例化类型
```cpp
// "Option<i32>" -> Option_i32
std::unordered_map<std::string, Type*> instantiated_types_;
```

3. **实例化算法**
```cpp
Type* instantiateGeneric(
    const std::string& template_name,
    const std::vector<Type*>& type_args
);
```

#### 3.2 TypeSystem扩展 (2-3小时)

**新增方法**:
```cpp
class TypeSystem {
public:
    // 注册泛型模板
    void registerGenericTemplate(GenericTemplate* tmpl);
    
    // 实例化泛型
    Type* instantiate(std::string name, std::vector<Type*> args);
    
    // 查找泛型模板
    GenericTemplate* lookupGenericTemplate(const std::string& name);
    
private:
    std::unordered_map<std::string, GenericTemplate*> generic_templates_;
    std::unordered_map<std::string, Type*> instantiated_types_;
};
```

**实现**:
```cpp
Type* TypeSystem::instantiate(std::string name, std::vector<Type*> args) {
    // 1. 生成实例化名称: "Option<i32>" -> "Option_i32"
    std::string instance_name = name;
    for (auto* arg : args) {
        instance_name += "_" + arg->toString();
    }
    
    // 2. 查找缓存
    if (auto it = instantiated_types_.find(instance_name)) {
        return it->second;
    }
    
    // 3. 查找泛型模板
    auto* tmpl = lookupGenericTemplate(name);
    if (!tmpl) return nullptr;
    
    // 4. 实例化类型（替换类型参数）
    Type* instance = instantiateFromTemplate(tmpl, args);
    
    // 5. 缓存并返回
    instantiated_types_[instance_name] = instance;
    return instance;
}
```

#### 3.3 Parser泛型使用解析 (1小时)

**解析泛型使用**:
```cpp
Type* Parser::parseType() {
    // ... 现有逻辑 ...
    
    // 解析泛型参数: Option<i32>
    if (match(TokenType::LESS)) {
        std::vector<Type*> type_args;
        do {
            type_args.push_back(parseType());
        } while (match(TokenType::COMMA));
        consume(TokenType::GREATER, "Expected '>'");
        
        // 返回泛型实例化请求
        return type_system_->instantiate(base_name, type_args);
    }
}
```

#### 3.4 Monomorphization Pass增强 (2-3小时)

**收集泛型使用**:
```cpp
class MonomorphizationPass {
    void visit(VariablePattern* node) {
        // 如果类型是泛型实例
        if (isGenericInstance(node->getType())) {
            collectInstantiation(node->getType());
        }
    }
    
    void collectInstantiation(Type* type) {
        // 记录需要实例化的泛型
        instantiations_.insert(type);
    }
};
```

**生成实例化代码**:
```cpp
void MonomorphizationPass::generateInstantiations() {
    for (auto* type : instantiations_) {
        auto* generic_type = static_cast<GenericInstanceType*>(type);
        
        // 生成具体类型定义
        // Option<i32> -> type Option_i32 = enum { Some(i32), None }
        generateConcreteType(generic_type);
    }
}
```

#### 3.5 CodeGen支持 (1-2小时)

**已有基础，主要是**:
- 处理实例化后的类型名称
- 确保LLVM类型映射正确

---

## 🔧 技术细节

### 类型推导算法

**Hindley-Milner简化版**:

1. **约束收集**:
```
let add: fn(i32, i32) -> i32 = (x, y) { x + y }
约束:
  - add的类型 = fn(i32, i32) -> i32
  - 闭包类型 = fn(?1, ?2) -> ?3
  - 统一: fn(?1, ?2) -> ?3 = fn(i32, i32) -> i32
```

2. **约束求解**:
```
?1 = i32
?2 = i32
?3 = i32
```

3. **类型替换**:
```
闭包参数 x: ?1 -> x: i32
闭包参数 y: ?2 -> y: i32
```

### 泛型实例化算法

**核心步骤**:

1. **模板收集** (Parser阶段)
```paw
type Option<T> = enum { Some(T), None };
// 注册为泛型模板
```

2. **使用识别** (TypeChecker阶段)
```paw
let opt: Option<i32> = ...
// 识别到泛型使用
```

3. **实例化** (Monomorphization Pass)
```
生成: type Option_i32 = enum { Some(i32), None };
注册到TypeSystem
```

4. **AST替换**
```
Option<i32> -> Option_i32（在AST中）
```

5. **CodeGen** (后端)
```
正常处理Option_i32（已经是具体类型）
```

---

## 📊 实施计划

### 今天（预计8小时+）

**09:00-10:00**: Phase 1 - 准备
**10:00-13:00**: Phase 2 - 类型推导
**14:00-19:00**: Phase 3 - 泛型系统
**19:00-20:00**: 测试和调试

### 里程碑

- [ ] M1: Parser支持无类型参数
- [ ] M2: TypeChecker基础推导
- [ ] M3: 泛型模板注册
- [ ] M4: 泛型实例化
- [ ] M5: 完整测试通过

---

**准备开始实施！** 🚀

