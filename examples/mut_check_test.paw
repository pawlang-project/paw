// 测试mut检查

type Point = struct {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    // mut对象可以修改
    let mut p1: Point = Point { x: 10, y: 20 };
    p1.x = 30;
    
    // 非mut对象不能修改（应该报错）
    let p2: Point = Point { x: 1, y: 2 };
    p2.x = 3;  // 错误！
    
    return p1.x;
}
