// 简单测试：看看纯 Paw 循环编译成什么

fn sum_to_n(n: i32) -> i32 {
    let mut sum: i32 = 0;
    let mut i: i32 = 0;
    
    loop i in 0..n {
        sum += i;
    }
    
    return sum;
}

fn main() -> i32 {
    return sum_to_n(10);
}

