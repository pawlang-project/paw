// Arithmetic operations example

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn multiply(a: i32, b: i32) -> i32 {
    return a * b;
}

fn main() -> i32 {
    let x: i32 = add(10, 20);
    let y: i32 = multiply(x, 2);
    return y;
}

