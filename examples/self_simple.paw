// 简单的Self类型测试

type Box = struct {
    value: i32,
    
    // 使用Self作为返回类型
    fn new(val: i32) -> Self {
        return Box { value: val };
    }
    
    fn get(self) -> i32 {
        return self.value;
    }
}

fn main() -> i32 {
    let b: Box = Box::new(42);
    return b.get();  // 42
}

