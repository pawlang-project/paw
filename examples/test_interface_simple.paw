// 接口系统 - 简化测试（不使用self）

type Display = interface {
    fn show();
};

type Point = struct {
    x: i32,
    y: i32
};

support Point with Display {
    fn show() {
        println("Showing Point");
    }
}

fn main() {
    println("Interface definition and support parsing successful!");
}

