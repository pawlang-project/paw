// 最简单的切片测试

fn sum(arr: [i32]) -> i32 {
    let mut total: i32 = 0;
    loop item in arr {
        total = total + item;
    }
    return total;
}

fn main() -> i32 {
    let arr: [i32] = [1, 2, 3];
    return sum(arr);  // 应该返回 6
}

