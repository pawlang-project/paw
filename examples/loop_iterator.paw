// 测试迭代器循环: loop item in array

fn main() -> i32 {
    let arr: [i32] = [10, 20, 30, 40, 50];
    let mut sum: i32 = 0;
    
    // 遍历数组
    loop item in arr {
        sum = sum + item;
    }
    
    return sum;  // 10+20+30+40+50 = 150
}

