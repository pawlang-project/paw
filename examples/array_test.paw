// 数组功能测试

fn main() -> i32 {
    // 数组字面量
    let arr: [i32; 5] = [1, 2, 3, 4, 5];
    
    // 数组索引访问
    let first: i32 = arr[0];
    let second: i32 = arr[1];
    
    let sum: i32 = first + second;
    
    return sum;
}

