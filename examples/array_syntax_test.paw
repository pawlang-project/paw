// 测试数组两种语法都支持

fn main() -> i32 {
    // 语法1：显式大小
    let explicit: [i32; 5] = [10, 20, 30, 40, 50];
    
    // 语法2：自动推导大小
    let inferred: [i32] = [1, 2, 3];
    
    let sum: i32 = explicit[0] + explicit[1] + inferred[0] + inferred[2];
    
    return sum;  // 10 + 20 + 1 + 3 = 34
}

