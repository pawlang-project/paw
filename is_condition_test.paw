type Option = enum {
    Some(i32),
    None(),
}

fn check_value(opt: Option) -> i32 {
    if opt is Some(x) {
        return x;
    }
    return -1;
}

fn main() -> i32 {
    let val: Option = Option::Some(50);
    let result: i32 = check_value(val);
    return result;  // 应该返回50
}
