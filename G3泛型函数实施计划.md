# G3: 泛型函数实施计划

**开始时间**: 2025-11-02  
**预计时长**: 5-8小时  
**当前进度**: G1,G4已完成，开始G3

---

## 🎯 目标

**支持语法**:
```paw
fn identity<T>(x: T) -> T {
    return x;
}

fn main() {
    let v1 = identity(42);  // 推导为identity<i32>(42)
    println(v1);  // 42
    
    let v2 = identity("hello");  // 推导为identity<string>("hello")
    println(v2);  // hello
}
```

---

## 📐 实现步骤

### 步骤1: 函数模板注册 (1小时)

**修改**: TypeSystem, GenericTemplate

**扩展GenericTemplate**:
```cpp
struct GenericTemplate {
    enum Kind {
        ENUM,
        STRUCT,
        FUNCTION  // 新增
    };
    
    union {
        EnumDecl* enum_def;
        StructDecl* struct_def;
        FunctionDecl* func_def;  // 新增
    };
    
    GenericTemplate(name, params, FunctionDecl* def)
        : kind(FUNCTION), func_def(def) {}
};
```

**修改Parser**:
```cpp
// parseFunctionDecl中区分泛型函数
if (!generic_params.empty()) {
    GenericTemplate* tmpl = new GenericTemplate(name, type_param_names, func_decl.get());
    type_system_->registerGenericTemplate(tmpl);
}
```

---

### 步骤2: 调用点类型参数推导 (2-3小时)

**修改**: TypeChecker::visit(CallExpr*)

**算法**:
```cpp
if (callee是泛型函数调用) {
    // 1. 获取参数类型
    std::vector<Type*> arg_types;
    for (auto& arg : call->getArgs()) {
        arg->accept(this);
        arg_types.push_back(arg->getType());
    }
    
    // 2. 推导类型参数
    std::vector<Type*> type_args = inferTypeArgs(func_tmpl, arg_types);
    
    // 3. 实例化函数
    FunctionDecl* instance = instantiateFunction(func_name, type_args);
}
```

---

### 步骤3: 函数实例化 (2-3小时)

**TypeSystem::instantiateFunction**:
```cpp
FunctionDecl* instantiateFunction(name, type_args) {
    // 1. 查找函数模板
    GenericTemplate* tmpl = lookupGenericTemplate(name);
    
    // 2. 替换类型参数
    std::unordered_map<string, Type*> substitution;
    for (size_t i = 0; i < type_args.size(); i++) {
        substitution[tmpl->type_params[i]] = type_args[i];
    }
    
    // 3. 实例化参数类型
    std::vector<Param> instantiated_params;
    for (auto& param : func_def->getParams()) {
        Type* param_type = substituteType(param.type, substitution);
        instantiated_params.push_back({param.name, param_type, param.is_mutable});
    }
    
    // 4. 实例化返回类型
    Type* return_type = substituteType(func_def->getReturnType(), substitution);
    
    // 5. 创建实例化的FunctionDecl
    FunctionDecl* instance = new FunctionDecl(
        instance_name,
        {},  // 无泛型参数
        instantiated_params,
        return_type,
        func_def->getBody()->clone(),  // 克隆函数体
        {},  // where子句
        func_def->isPublic()
    );
    
    return instance;
}
```

---

### 步骤4: Monomorphization集成 (1-2小时)

**修改**: MonomorphizationPass

- 遍历所有函数调用
- 实例化需要的泛型函数
- 生成具体代码

---

## 🚧 技术挑战

### 挑战1: 函数体克隆
函数体AST需要深拷贝，可能需要实现AST clone机制

**解决方案**: 暂时保存原始AST，实例化时共享

---

### 挑战2: 类型参数推导
从参数类型推导泛型参数，需要统一算法

**解决方案**: 简单的位置匹配，T对应第一个参数类型

---

### 挑战3: CodeGen集成
需要为每个实例化函数生成LLVM IR

**解决方案**: 在CodeGen阶段处理实例化函数

---

## 📋 简化策略

**Phase 1**: 仅支持显式类型参数
```paw
let v = identity<i32>(42);  // 显式指定T=i32
```

**Phase 2**: 支持类型参数推导
```paw
let v = identity(42);  // 推导T=i32
```

先实现Phase 1，再实现Phase 2

---

## 🎯 开始实施...

