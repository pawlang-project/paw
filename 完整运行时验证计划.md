# 完整运行时验证计划

**目标**: 证明泛型系统不仅语法正确，而且运行时完全可用

---

## 🧪 测试套件

### 测试1: 基础泛型enum值验证 ⏳
```paw
type Option<T> = enum {
    Some(T),
    None
}

fn get_value(opt: Option<i32>) -> i32 {
    match opt is {
        Some(v) => { return v; },
        None => { return -1; }
    }
}

fn main() {
    let opt1: Option<i32> = Option::Some(42);
    let v1 = get_value(opt1);
    println(v1);  // 期望输出: 42
    
    let opt2: Option<i32> = Option::Some(100);
    let v2 = get_value(opt2);
    println(v2);  // 期望输出: 100
}
```

**验证点**:
- ✅ 泛型enum构造
- ✅ 泛型enum作为参数传递
- ✅ Pattern matching正确匹配
- ✅ 返回值正确提取

**期望输出**:
```
42
100
```

---

### 测试2: 多类型实例化独立性 ⏳
```paw
fn test_i32() -> i32 {
    let opt: Option<i32> = Option::Some(42);
    match opt is {
        Some(v) => { return v; },
        None => { return -1; }
    }
}

fn test_bool() -> bool {
    let opt: Option<bool> = Option::Some(true);
    match opt is {
        Some(v) => { return v; },
        None => { return false; }
    }
}

fn main() {
    let val = test_i32();
    println(val);  // 期望: 42
    
    let b = test_bool();
    if b {
        println("true");  // 期望: true
    }
}
```

**验证点**:
- ✅ Option<i32>和Option<bool>独立实例化
- ✅ 不同类型的variant数据正确存储
- ✅ Pattern matching对不同实例化类型正确工作

**期望输出**:
```
42
true
```

---

### 测试3: None variant验证 ⏳
```paw
fn maybe_value(has_value: bool) -> Option<i32> {
    if has_value {
        return Option::Some(123);
    } else {
        return Option::None;
    }
}

fn main() {
    let opt1 = maybe_value(true);
    match opt1 is {
        Some(v) => { println(v); },  // 期望: 123
        None => { println("Got none"); }
    }
    
    let opt2 = maybe_value(false);
    match opt2 is {
        Some(v) => { println(v); },
        None => { println("Got none"); }  // 期望: Got none
    }
}
```

**验证点**:
- ✅ None variant构造
- ✅ None variant匹配
- ✅ 控制流正确

**期望输出**:
```
123
Got none
```

---

### 测试4: Result<T>双参数泛型 ⏳
```paw
type Result<T> = enum {
    Ok(T),
    Err(string)
}

fn divide(a: i32, b: i32) -> Result<i32> {
    if b == 0 {
        return Result::Err("Division by zero");
    } else {
        return Result::Ok(a / b);
    }
}

fn main() {
    let r1 = divide(10, 2);
    match r1 is {
        Ok(v) => { println(v); },  // 期望: 5
        Err(msg) => { println(msg); }
    }
    
    let r2 = divide(10, 0);
    match r2 is {
        Ok(v) => { println(v); },
        Err(msg) => { println(msg); }  // 期望: Division by zero
    }
}
```

**验证点**:
- ✅ 双参数泛型
- ✅ Err variant (string类型)
- ✅ Ok variant (i32类型)
- ✅ 运行时值正确

**期望输出**:
```
5
Division by zero
```

---

## 📊 测试计划

| 测试 | 验证目标 | 预计时间 |
|-----|---------|---------|
| 测试1 | 基础功能 | 15分钟 |
| 测试2 | 多类型实例化 | 15分钟 |
| 测试3 | None variant | 15分钟 |
| 测试4 | 双参数泛型 | 30分钟 |
| **总计** | **完整验证** | **1-1.5小时** |

---

## 🎯 开始执行测试...

